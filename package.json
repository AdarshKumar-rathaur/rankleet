{
  "name": "rankleet",
  "version": "1.0.0",
  "description": "A competitive LeetCode ranking platform",
  "main": "server/server.js",
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "cd server && npm install && NODE_ENV=production npm start",
    "dev": "concurrently \"cd server && npm run dev\" \"cd client && npm run dev\"",
    "install-server": "cd server && npm install",
    "install-client": "cd client && npm install",
    "postinstall": "npm run install-server && npm run install-client"
  },
  "keywords": ["leetcode", "ranking", "competitive", "react", "express"],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}